import { Transition } from '@/components/shared/preloader';
import React from 'react';

const EspaceDarja = () => {
  return (
    <Transition>
      <div>EspaceDarja</div>
    </Transition>
  );
};

export default EspaceDarja;
