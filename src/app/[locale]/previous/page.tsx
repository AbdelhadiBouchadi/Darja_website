import { Transition } from '@/components/shared/preloader';
import React from 'react';

const Previously = () => {
  return (
    <Transition>
      <div>Previously</div>
    </Transition>
  );
};

export default Previously;
